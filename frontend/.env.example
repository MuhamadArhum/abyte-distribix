# Base URL the frontend calls for the backend API.
# Leave unset for the current Electron desktop packaging (the backend is
# always spawned locally on this port) — only set this for a web-hosted
# or remote-backend deployment.
VITE_API_BASE_URL=http://localhost:3009/api
